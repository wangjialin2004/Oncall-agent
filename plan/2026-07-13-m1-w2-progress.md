# M1 W2 进度 — Context / Checkpoint / Evidence / CI

> 日期：2026-07-13  
> 计划：[2026-07-13-m1-w2-context-checkpoint-evidence-ci.md](./2026-07-13-m1-w2-context-checkpoint-evidence-ci.md)  
> 状态：**W2 主干已合入**（ci-smoke 相关单测绿）

## 已完成

| WP | 内容 | 状态 |
|---|---|---|
| 计划 | W2 实施计划落盘 + AGENTS 索引 | ✅ |
| WP-A2 | `required_evidence` 类型 → 工具细匹配；`HARNESS_EVIDENCE_MATCH_ENABLED` | ✅ |
| WP-CTX1 | complete 前 `_stamp_current_turn` 刷新 recent_turns + last_answer_summary | ✅ |
| WP-I1-a | 外层 timeout best-effort checkpoint（runtime holder） | ✅ |
| WP-I1-b | resume 事件 payload 带 `context_version` / `context_snapshot_ref` | ✅ |
| WP-I1-c | 幂等白名单扩大只读工具集 | ✅ |
| WP-F2 | `make ci-smoke` + `.github/workflows/ci-smoke.yml` | ✅ |

## 新开关

```text
HARNESS_EVIDENCE_MATCH_ENABLED=true
```

## 关键文件

- `app/agent/harness/verifier.py`
- `app/agent/harness/loop.py`
- `app/services/harness_checkpoint.py`
- `app/config.py` / `.env.example`
- `Makefile` / `.github/workflows/ci-smoke.yml`
- `tests/test_m1_w2_context_checkpoint.py`

## 验证

```bash
python -m pytest \
  tests/test_m1_close_the_loop.py \
  tests/test_m1_w2_context_checkpoint.py \
  tests/test_harness_verifier.py \
  tests/test_harness_checkpoint.py \
  tests/test_context_integration.py \
  -q --no-cov
# 37 passed
```

或：`make ci-smoke`

## 未做（W3+）

- WP-A3 mid-loop replan  
- WP-L1 时延（planner 轻量模型对照）  
- 评测扩到 ≥18/23  
- ContextState 从 ref 的更深层 rehydrate（当前依赖 store get_or_rebuild + ref 元数据）  

## 建议下一步（W3）

1. WP-A3 replan  
2. WP-L1 时延基线对照  
3. 本机 10 题 eval 回归 re-evidence + multi-turn  
