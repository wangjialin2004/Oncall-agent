# M3 W11 Progress: Observability · Online · Dual-path · OTEL

Date: 2026-07-15  
Plan: [2026-07-15-m3-w11-observability-online.md](./2026-07-15-m3-w11-observability-online.md)  
Status: **主干已合入**（单测绿；未宣布 L3）

## Implemented

### WP-W11-1 成本 / 工具指标
- `agent_tokens_total{role}` — prompt / completion / total（来自 `state.usage_total`）
- `agent_tool_calls_total{tool,status}` — tool 白名单 + status∈ok/error/timeout/other
- Helpers: `observe_agent_tokens` / `observe_tool_call` / `observe_tool_calls_from_timeline`
- Complete 路径：`loop.py` 扫描 timeline 一次 + 记 tokens
- 文档：[docs/pilot/cost-dashboard.md](../docs/pilot/cost-dashboard.md)

### WP-W11-5 Trace 采样
- `HARNESS_TRACE_SAMPLE_RATE`（默认 **0.0**；仅 enabled 时生效）
- payload 增补：`distill_draft` / `anti_pattern` 摘要 + compact `usage`
- W6 回归：`test_trace_export_writes_when_enabled` 强制 sample_rate=1.0

### WP-W11-2 Online 抽样
- [docs/pilot/online-eval-template.md](../docs/pilot/online-eval-template.md)
- `scripts/sample_online_runs.py`（`--dry-run` / `--from-traces`）
- `docs/pilot/samples/` 输出骨架

### WP-W11-3 双路径收敛（H4）
- [docs/pilot/context-dual-path.md](../docs/pilot/context-dual-path.md)
- config / `.env.example`：stateful 主路径；legacy = rebuild/fallback only（不删 ContextBuilder）

### WP-W11-4 OTEL 可选（最小方案 A+B 接线）
- `OTEL_EXPORTER_OTLP_ENDPOINT` 空 = 零行为
- `app/agent/harness/otel_export.py` best-effort；无 SDK 则 skip
- ADR：[docs/pilot/otel-optional.md](../docs/pilot/otel-optional.md)

## Defaults / rollback

```text
HARNESS_TRACE_EXPORT_ENABLED=false
HARNESS_TRACE_SAMPLE_RATE=0.0
OTEL_EXPORTER_OTLP_ENDPOINT=
HARNESS_STATEFUL_CONTEXT_ENABLED=true
# tokens/tools counters are append-only and safe
```

## Verification

```bash
python -m pytest \
  tests/test_m3_w11_metrics_cost.py \
  tests/test_m3_w11_trace_sample.py \
  tests/test_m3_w11_context_path.py \
  tests/test_m3_w9_hitl_metrics.py \
  tests/test_m3_w10_distill.py \
  tests/test_m2_w6_parallel_delegation.py::test_trace_export_writes_when_enabled \
  tests/test_m2_w6_parallel_delegation.py::test_trace_export_noop_when_disabled \
  -q --tb=line --no-cov
# 25 passed

python scripts/sample_online_runs.py --n 3 --dry-run
# Wrote docs/pilot/samples/online_sample.{csv,md}
```

## Exit criteria checklist

| # | 标准 | 状态 |
|---|---|---|
| 1 | tokens/tools 指标代码 + complete 打点 | ✅ |
| 2 | cost-dashboard PromQL 可复制 | ✅ |
| 3 | online 模板 + dry-run 脚本 | ✅ |
| 4 | 双路径文档；stateful 默认不回归 | ✅ |
| 5 | OTEL 空 endpoint 零行为 + ADR | ✅ |
| 6 | 单测绿 | ✅ 25 passed（含 W9/W10 抽样） |
| 7 | **不**宣布 L3 | ✅ |

## Non-goals (unchanged)

- Grafana 正式集群 / 完整 SSO
- 默认全量 trace / OTEL
- 自动处置 / 静默 AUTO_DISTILL
- full 23 强制重跑
- 物理删除 ContextBuilder

## Next

- **W12**：全量回归 + 北极星 + **L3 出口评审**（另开计划，不在本批准自动开工）
- 可选：OTEL 真 OTLP exporter；online 实填 1 轮人工分
