# M3 W10 Progress: Distill · Anti-pattern · Clarify · Compose

Date: 2026-07-14  
Plan: [2026-07-14-m3-w10-learn-clarify-compose.md](./2026-07-14-m3-w10-learn-clarify-compose.md)

## Implemented

### WP-W10-1 半自动蒸馏
- Config: `LONG_TERM_MEMORY_DISTILL_ENABLED` / `AUTO_DISTILL=false` / `DISTILL_REQUIRE_CONFIRM=true` / min confidence
- Schema: `experience_memories.status` (`pending|active|rejected`) + migration
- Service: `create_draft_from_run` / `confirm_draft` / `reject_draft` / `list_pending`
- API: `POST /api/memory/distill/confirm|reject` · `GET /api/memory/distill/pending`
- Harness complete hook: `_maybe_distill_experience` → complete payload `distill_draft`
- Frontend: DistillCard + confirm/reject; adopt feedback prefers draft confirm

### WP-W10-2 失败反模式
- Config: `HARNESS_ANTI_PATTERN_CAPTURE_ENABLED` / `HARNESS_ANTI_PATTERN_TOOL_HINT`
- Service: `create_anti_pattern` (`source_type=anti_pattern`)
- Harness: `_maybe_capture_anti_pattern` on failed investigation tools / zero-evidence gap
- Recall: anti-pattern ranked after success; tool text marks「反模式/勿重复」

### WP-W10-3 澄清结构化
- `_emit_clarification` yields `decision_event` stage=clarify + complete `missing_params` / `clarification`
- Frontend: clarify chips in ChatWorkspace composer

### WP-W10-4 Compose 草案
- `deploy/compose/docker-compose.pilot.yml` + `Dockerfile.backend`
- `docs/pilot/deploy-checklist.md` 更新（bat 主路径 + compose 草案 + W10 开关）

## Verification (unit)

```bash
python -m pytest \
  tests/test_m3_w10_distill.py \
  tests/test_m3_w10_anti_pattern.py \
  tests/test_m3_w10_clarify.py \
  tests/test_m3_w9_hitl_metrics.py \
  -q --tb=line --no-cov
# 16 passed
```

## Live HTTP smoke（2026-07-15）

### A. 半健康（Milvus 未起，health 503）
| 步骤 | 结果 |
|---|---|
| OpenAPI distill 三路由 | ✅ |
| pending → confirm → active | ✅ `exp-1f01b290…` |

### B. 全栈健康（Docker Desktop + milvus + MCP + backend 重启）
| 组件 | 状态 |
|---|---|
| Redis | listen |
| MCP cls/monitor | `:8003` / `:8004` reachable |
| Milvus standalone | healthy `19530`（`vector-database.yml` 起 etcd/minio/standalone，跳过 attu 拉镜像失败） |
| Backend | `/health` **200 healthy** · milvus connected · mcp reachable |
| 单测 W9/W10 | **23 passed** |

| 步骤 | 结果 |
|---|---|
| 登录 | ✅ admin |
| `POST /api/assistant` 诊断 | ✅ **109.7s** · route=diagnosis · **1982** SSE events · answer~3380 chars |
| complete.`distill_draft` | ✅ pending `exp-4ef2fb6f…` · requires_confirm |
| complete.`anti_pattern` | ✅ 同步写出（prometheus 工具失败） |
| pending list | ✅ 1 条 |
| confirm | ✅ active / executed=false |
| get experience | ✅ enabled · source_type=auto_distill |
| pending after confirm | ✅ 0 |

样例：`session=w10-live-3449699ff6` · draft `exp-4ef2fb6ffb534d3b8f032f40fee22014`

## Defaults / rollback

```text
LONG_TERM_MEMORY_DISTILL_ENABLED=true
LONG_TERM_MEMORY_AUTO_DISTILL=false
LONG_TERM_MEMORY_DISTILL_REQUIRE_CONFIRM=true
HARNESS_ANTI_PATTERN_CAPTURE_ENABLED=true
```

Rollback: set distill/anti flags false (see plan).

## Not done / follow-ups
- full/minimal 评测未在本切片强制重跑（未改评分语义；complete 仅增字段）
- Milvus 未起时 health 503（蒸馏/confirm 不依赖 Milvus）
- WP-D3 偏好迁移仍非目标
- L3 / L2 书面改判未做；W11 计划未写

## 变更记录
| 日期 | 说明 |
|---|---|
| 2026-07-14 | 主干合入；单测 16 passed |
| 2026-07-15 | HTTP live smoke：pending → confirm → active（Milvus 缺席 health 503） |
| 2026-07-15 | 全栈拉起（MCP+Milvus+backend）+ 健康 live：**109s diagnosis**，draft/anti/confirm 全绿；单测 23 passed |
