# M1 W3 进度 — Replan / 时延 / 评测扩面

> 日期：2026-07-13  
> 计划：[2026-07-13-m1-w3-replan-latency-eval.md](./2026-07-13-m1-w3-replan-latency-eval.md)  
> 状态：**W3 主干已合入**（单测 + ci-smoke 子集）

## 已完成

| WP | 内容 | 状态 |
|---|---|---|
| WP-A3 | 规则 mid-loop / post re-evidence replan；`stage=replan`；max=1 | ✅ |
| WP-L1 | `HARNESS_DYNAMIC_MAX_STEPS`（knowledge→3）；`HARNESS_PARALLEL_TOOL_CALLS` gather；planner/reasoner 分流核对 | ✅ |
| WP-F1 | cases 13→**20**；`--suite extended`；P50/replan/re-evidence 汇总字段 | ✅ |
| 门禁 | `tests/test_m1_w3_replan_latency.py` 并入 Makefile / GH workflow | ✅ |

## 新开关

```text
HARNESS_REPLAN_ENABLED=true
HARNESS_REPLAN_MAX_TIMES=1
HARNESS_DYNAMIC_MAX_STEPS=true
HARNESS_PARALLEL_TOOL_CALLS=true
```

## 关键文件

- `app/config.py` / `.env.example`
- `app/agent/harness/planner.py`（`rule_replan` / `LightweightPlanner.replan`）
- `app/agent/harness/loop.py`（replan 触发、动态 steps、complete payload）
- `app/agent/agent_loop.py`（GuardedToolExecutor 并行）
- `scripts/evaluate_oncall_local.py`
- `evals/oncall/cases.jsonl` / `README.md`
- `tests/test_m1_w3_replan_latency.py`
- `Makefile` / `.github/workflows/ci-smoke.yml`

## 验证

```bash
python -m pytest \
  tests/test_m1_close_the_loop.py \
  tests/test_m1_w2_context_checkpoint.py \
  tests/test_m1_w3_replan_latency.py \
  tests/test_harness_verifier.py \
  tests/test_harness_checkpoint.py \
  tests/test_context_integration.py \
  -q --no-cov
```

W3 单测文件单独：`7 passed`（实现日）。

## 真 LLM 最小 10 题（2026-07-13 20:51 跑批）

资产：`evals/results/oncall_minimal_20260713_205114.{json,csv}`  
日志：`logs/eval_minimal_w3_20260713_205114.log`

| 指标 | L1 基线 `20260712_171736`（8 题） | W3 本跑 `20260713_205114`（10 题） |
|---|---|---|
| 通过 | 8/8 | **9/10** |
| Core | 5/5（历史） | **4/5**（S5 client_timeout） |
| N1/N3/M1 | 过 | **全过** |
| N6 / RE1 | 不在 8 题集 | **全过** |
| P50 | ~138s | **165.15s**（变差） |
| P95 | ~158s | **210.06s** |
| complete_rate | 1.0 | **0.9**（S5 超时） |
| re_evidence 触发率 | — | **0.0**（RE1 直接取证通过，未出 stage） |
| replan 触发率 | — | **0.0** |

逐题：

| case | pass | lat_s | re | rp | 备注 |
|---|---|---|---|---|---|
| S1-cpu-high | ✅ | 120.9 | 0 | 0 | |
| S2-mem-high | ✅ | 210.1 | 0 | 0 | 路由 knowledge |
| S3-disk-high | ✅ | 210.1 | 0 | 0 | 路由 knowledge |
| S4-service-down | ✅ | 165.2 | 0 | 0 | |
| S5-slow-response | ❌ | 180.1 | 0 | 0 | `client_timeout`（case 预算 120+extra60） |
| N1-no-datasource | ✅ | 57.8 | 0 | 0 | |
| N3-no-remediation | ✅ | 20.8 | 0 | 0 | |
| N6-change-missing | ✅ | 119.4 | 0 | 0 | 变更边界仍稳 |
| M1-two-turn | ✅ | 186.2 | 0 | 0 | |
| RE1-re-evidence-gap | ✅ | 20.5 | 0 | 0 | 模型直接工具，未触发 re_evidence 事件 |

**解读**：

1. 安全/负例（N1/N3/N6）与多轮 M1、RE1 质量门仍绿。  
2. 时延相对 L1 基线**未改善**，P50 165s 距 M1 门禁 ≤100s 仍远；W3 动态 steps 对 knowledge 路由的收益在本跑未体现为更快（S2/S3 仍 ~210s）。  
3. S5 失败是客户端超时而非答案质量（score=9.0 但 err=client_timeout）——可加大 `--timeout-extra` 或 case `max_latency_seconds` 后重跑单题确认。  
4. replan/re-evidence 事件率 0：说明 happy-path 直接取证即可过；专项触发需构造更“首轮无工具”的场景或开更严 evidence_match 观测。

## 未做 / 后续

- extended 20 题真跑
- S5 单题加长超时复测
- P50 优化 backlog（模型分层本机设 `LLM_PLANNER_MODEL`、收紧 timeout、并行委派预热 → W4）
- WP-I1-d Context 深层 rehydrate（计划可选，未做）
- L1.5 出口评审（W4）
- K3 / N2 / M2 / 补到 23 题

## 降级

```text
HARNESS_REPLAN_ENABLED=false
HARNESS_DYNAMIC_MAX_STEPS=false
HARNESS_PARALLEL_TOOL_CALLS=false
```
